# Data Contracts Specification

Every exchange requires an explicit DataContract defining provider, consumer, dataset, purpose, scope, allowed fields, geography, time range, frequency, retention, consent, and security requirements.
