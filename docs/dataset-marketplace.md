# Dataset Marketplace & Synthetic Data Framework

Integrated with Stage 23 Data Commons. Provides controlled synthetic datasets across 15 agricultural domains carrying `synthetic=true` and generator seed metadata. Synthetic data is never represented as real farmer data.
