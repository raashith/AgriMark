# AgriMark Web

Next.js web application for AgriMark. It connects to the canonical FastAPI API at `/api/v1`.

Vercel deployment root directory: `web`.

Required environment variable:

`NEXT_PUBLIC_API_BASE_URL=https://agrimark-api.onrender.com/api/v1`
