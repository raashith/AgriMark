# Stitch static landing/source pages

The full Stitch HTML export is preserved in `stitch-assets/` as source material. The production interactive app lives in `stitch-app/`.

This directory is intentionally reserved for static assets that are safe to serve directly from the Next.js public folder.
